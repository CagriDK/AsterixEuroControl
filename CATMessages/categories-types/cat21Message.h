#pragma once

#include <vector>
#include <string>

#pragma pack(push, 1) // Hizalamayı 1 byte'a ayarla

struct Cat21Record
{
    Cat21Record() {};
};

#pragma pack(pop) // Önceki hizalama ayarını geri yükle